<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <p><%= link_to task.title, "/tasks/#{task.id}" %></p>
    </li>
  <% end %>
</ul>

<p><%= link_to 'Create new Task', "/tasks/new" %></p>
