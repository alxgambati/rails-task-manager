<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<p><%= link_to 'Edit this Task!', "/tasks/edit/#{@task.id}" %></p>
<p></p>
<p><%= link_to 'Delete this Task!', "/tasks/#{@task.id}", method: "delete" %></p>
<p><%= link_to 'Back to the list!', "/" %></p>
